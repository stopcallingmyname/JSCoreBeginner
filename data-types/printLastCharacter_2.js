function printLastCharacter(str) {
  const lastCharacter = str.charAt(str.length - 1);
  console.log('last symbopl:', lastCharacter);
}

const myString = 'lorem ipsum dolor sit amet';
printLastCharacter(myString);
